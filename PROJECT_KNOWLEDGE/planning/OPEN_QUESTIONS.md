# Open Questions

- Which directory should become the authoritative Git repository?
- Should release binaries be distributed through GitHub Releases rather than committed?
- What is the authoritative live database/library fixture for integration testing?
- Which local HTTP interfaces may bind beyond loopback, if any?
- Which credential-storage mechanism is acceptable for the owner's Windows environment?
- Which central-manifest slice should be implemented first?
