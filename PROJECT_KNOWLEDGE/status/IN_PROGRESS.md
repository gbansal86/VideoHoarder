# In Progress

- One five-video load test passed at `parallel_videos=5` and `concurrent_fragments=4`; broader testing across slower connections and rate-limited hosts is still needed before changing shipped defaults.
- Windows packaged/PySide click-through and live-network acceptance remain pending; source tests do not replace installed-EXE validation.
