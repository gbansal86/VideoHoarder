# Git Repository Completeness

Recorded: 2026-08-15
Status: Initial source baseline established

Included: application modules, entry point, config template/current non-inline-key config, tests, intentional test fixtures, requirements, PyInstaller/build/install scripts, build helpers, specifications/checkpoints/future requests, selected design assets, and source-support tools.

Excluded intentionally: `api_key.txt`, `urls.txt`, media/downloads/transcripts, application data and databases, logs, maintenance/runtime output, build/dist outputs, ZIP releases, browser profiles/caches/session databases, document render QA, Python caches, and test runtime output.

Known limitation: there is no pre-baseline Git history. Release binaries remain locally available but are not committed.
