Master modular sequential grouped mode: upload each package's single CHATGPT_PACKAGE.json file in a fresh ChatGPT chat. When a validated planner supplies parent_groups, VideoHoarder creates one package per parent group, not one package per small topic subgroup. Only transcript/SRT-backed videos are packaged in compact mode. Each package embeds VIDEO_LIBRARY_MASTER_PROMPT_MODULAR_SEQUENTIAL.md as master_prompt_exact and instructs ChatGPT to follow it as authoritative. Viewer-comment intelligence is excluded because that master prompt says not to use comments for transcript analysis. Adaptive batching uses the master prompt standard of up to 25 similar-topic videos per package, with transcript character count as the real safety limit.
