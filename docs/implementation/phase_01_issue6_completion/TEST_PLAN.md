# Test Plan

1. Baseline full unittest run.
2. Unit tests: duplicate active-library guard, metadata-only non-blocking, queue duplicate detection.
3. Unit tests: physical media available vs stale downloaded flag.
4. Source-contract tests: dedicated Queue page, scrollbars, refresh button, queued reason, stacked Job Details controls, terminal stats refresh, packaged fixture.
5. Existing full regression suite.
6. Python compileall.
7. Windows native GUI execution: required before release completion; not executable in current Linux environment.
