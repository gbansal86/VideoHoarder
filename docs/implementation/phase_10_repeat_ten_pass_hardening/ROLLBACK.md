# Rollback

Rollback only by restoring the prior Phase-09 versions of the changed source files together. If rolled back, rerun the full Phase-09 suite and restore its final-validation evidence. Do not roll back only the collision checks because that reintroduces destructive data-loss behavior.
