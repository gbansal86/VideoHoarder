# Code Changes

- Atomic settings validation/persistence.
- Per-run localhost mutation token plus loopback Host/Origin/client validation.
- Strict bounded JSON object parsing.
- Correct single/suffix byte-range parsing.
- Per-video SQLite SAVEPOINT migration atomicity.
