# Phase 04 Rollback

Restore the former BUILD_WINDOWS launcher workflow only together with the former documentation/feature classification. Do not claim a self-contained release if reverting to VideoHoarder_launcher.spec. `BUILD_DEV_LAUNCHER.ps1` provides the legacy development-launcher path without requiring a rollback.
